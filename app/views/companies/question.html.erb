<% provide(:title, @company.name + " | Questions") %>

<div class="container question">
  <div class="row title">
    <p id="notice"><%= notice %></p>

    <h1 id="company-name"><%= link_to @company.name, company_url(@company) %> Questions</h1>
    <div data-toggle="collapse" data-target="#question" id="plus-icon-question">
          <i class="foundicon-plus"></i></div>
  </div>


  <hr class="company-divider">

  <div id="question" class="collapse">
    <div class="row search">

      <div id="question-form" title="Write a new question">
        <input id="question-content" name="question-content" class="text ui-widget-content ui-corner-all"/><br/>
        <input id="question-company" name="question-company" type="hidden" value="<%= @company.id %>" /><br/>
        <a id="add-question" class="btn spacer">Ask Question</a>
      </div>

    </div>

    <hr class="company-divider">
  </div>

<div id="accordian">
  <% if @questions.any? %>
      <% @questions.each do |question| %>
          <h3 id="question-cont">
          <%= question.content %><br/>
          <span id="question-date"><%= question.created_at %></span>
          </h3>

          <div>
          <% if question.answers.any? %>
              <% question.answers.each do |answer| %>
                  <div id="answer-cont"><%= answer.content %>
                    <span id="answer-date"><%= answer.created_at %></span>
                  </div>
              <% end %>
          <% end %>
          <%= form_tag(:controller => "questions", :action => "create_answer", :class => "add-answer-btn") do %>
            <%= text_field_tag 'answer_content', nil, :placeholder => "Your answer...", :class => "add-answer-btn" %>
            <%= text_field_tag 'answer_question', nil, :type => "hidden", :value => question.id %>
            <%=  submit_tag("Add", :class => "btn spacer add-answer-btn") %>
          <% end %><br/>
          </div>

      <% end %>
  <% end %>
</div>

</div>