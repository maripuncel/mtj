<% provide(:title, 'Home') %>

<div class="row">
<div class="span7">
  <div id="info">
    <h2>MIT is hard. <br/> Finding a job should be easy.</h2>
    <div id="image_container">
    <%= image_tag "home_icon.jpg", :id=>"home_icon" %>
    </div>
    <p>View information about interviews and job offers.
    Our goal is to tailor company information to MIT students and ease the process of job hunting.</p>
    <br/>
    <% if !@current_user %>
    <span id="join">Register with your MIT email address <i class="foundicon-right-arrow" id="home_arrow"></i></span>
    <% end %>
  </div>
</div>

<% if !@current_user %>

  <div class="span5">
    <div id="registration">
      <h4>Registration</h4>
      <p>Only users valid MIT addresses can register.</p>
      <form class="form-signin" accept-charset="UTF-8" action="/create_account" method="post">
        <div class= 'field' id="input-block-email">
          <%= label_tag :email %>
          <%= text_field_tag :email, params[:email] %>
        </div>
        <div class= 'field' id="input-block-password">
          <%= label_tag :password %>
          <%= password_field_tag :password %>
        </div>
        <div class= 'field'  id="input-block-confirm-password">
          <%= label_tag :confirm_password %>
          <%= password_field_tag :confirm_password %>
        </div>
        <div class = 'actions'>
          <%= submit_tag 'Register', :id=>"reg-btn" %>
        </div>
      </form>
    </div>
  </div>


<% end %>

<% if @current_user %>


  <div class="span5">
    <div id="company_search">

          <h4>Search for companies</h4>
          <%= form_for(Company.new) do |f| %>
              <input id ="company_name" name="company[name]" type="text" placeholder="Company Name" autocomplete="off" />
              <input type="submit" id="add_button" value="Add Company" name="commit">
          <% end %>

    <div id="companies", style="width:300px;height:250px;overflow:auto;", class="well well-small">
    </div>
       

    <%= javascript_tag  do %>
        
	window.onload=function(){load()};
        $('#company_name').keyup(function () {
        load();
        });



    <% end %>
    </div></div>


<% end %></div>


