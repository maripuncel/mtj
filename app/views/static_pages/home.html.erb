<% provide(:title, 'Home') %>

<% if !@current_user %>
    <h4> The one and only MIT company, job, and interview resource! </h4>
<% end %>
<br>
<% if @current_user %>

    <div id="home_carousel" class="carousel slide">
      <div class="carousel-inner">
        <div class="item active">
          <img src="#" alt="">
          <div class="container">
            <div class="carousel-caption">
              <h1>Microsoft</h1>
              <p class="lead">
                "blah blah"
              </p>
              <a class="btn btn-large btn-primary" href="#">More info</a>
            </div>
          </div>
        </div>
        <div class="item">
          <img src="#" alt>
          <div class="container">
            <div class="carousel-caption">
              <h1>Apple</h1>
              <p class="lead">
                "blah blah"
              </p>
              <a class="btn btn-large btn-primary" href="#">More info</a>
            </div>
          </div>
        </div>
      </div>
      <a class="left carousel-control" href="#home_carousel" data-slide="prev"></a>
      <a class="right carousel-control" href="#home_carousel" data-slide="next"></a>
    </div>

    <div class="container home_content">
      <div class="row">
        <div class="span8">
          <h4>Search for companies</h4>
          <%= form_for(Company.new) do |f| %>
              <input id ="company_name" name="company[name]" type="text" placeholder="Company Name" autocomplete="off" />
              <input type="submit" id="add_button" value="Add Company" name="commit">
          <% end %>
<% end %>

<div id="companies">
</div>

<%= javascript_tag  do %>

    $('#company_name').keyup(function () {
    load();
    });

    $('#add_button').hide();


<% end %>

<br>
</div>
<div class="span4">

  <% if @current_user %>
      <%= link_to "Show all companies", companies_url %>
  <% end %>
</div>
</div>
</div>
